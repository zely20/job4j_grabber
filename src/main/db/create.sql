create table posts (
                       id serial,
                       name text,
                       topic_text text,
                       lint text,
                       date_creted date
);